spring:
  datasource:
    url: jdbc:mysql://prod-server:3306/smart_backend
    username: prod
    password: ${MYSQL_PROD_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8082
